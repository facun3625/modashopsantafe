services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: modashop
      POSTGRES_PASSWORD: modashop
      POSTGRES_DB: modashop
    ports:
      - "5439:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
